#ifndef RENDER_THEME_H
#define RENDER_THEME_H

#include <QString>

struct RenderTheme {
  QString textColor;
  QString activeLineTextColor;
  QString accentColor;
  QString highlightColor;
};

#endif
